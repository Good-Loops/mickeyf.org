/**
 * @packageDocumentation
 *
 * Animations support public surface.
 *
 * Responsibility:
 * - Reusable support types/helpers shared across animation engines.
 *
 * Non-responsibilities:
 * - Specific animation implementations (Mandelbrot/Tree/etc.).
 */

export type {
	Vec2,
	TourOutput,
	TourDurations,
	TourZoomTargets,
	TourPresentation,
} from '../animations/dancing fractals/fractals/mandelbrot/MandelbrotTourTypes';
